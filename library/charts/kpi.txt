# Big number — one figure that a person is actually looking for. It gets the
# size; everything else on the card is context for it.

## the card — figure, delta, trend, in that order
 ╭─ revenue · this month ────────────╮
 │                                   │
 │   ¥ 1,240,800                     │
 │   ▲ 8.2%  vs last month           │
 │   ▁▂▄▃▅▆▇  30d                    │
 ╰───────────────────────────────────╯

## a row of them — one scale, one width, one baseline
 revenue        orders         refunds        p99
 ¥ 1.24M        318            2.1%           486 ms
 ▲ 8.2%         ▲ 2.6%         ▲ 0.4%  ⚠      ▲ 121%  ✗
 ▁▂▄▃▅▆▇        ▂▃▃▅▄▆▇        ▁▁▂▂▃▄▅        ▁▁▂▂▃▅▇

## the figure alone, when the context is in the heading
      18,402
      requests · last hour

## with its target — the only time a second number may be as large
      68%        of  100%       error budget spent
      ▰▰▰▰▰▰▰▱▱▱                9 days left

## states a big number has to be able to say
      ¥ 1.24M          a value
      —                no data yet (an em dash, never a zero)
      0                actually zero (which is a fact, not an absence)
      ¥ 1.24M ⟳        stale, refreshing
      ✗ unavailable    the query failed — never show the last good number
                       without saying that is what you are looking at

# paint: the figure -> text.heading at 2–3× body · the unit -> text.muted
# the delta arrow states direction only; the ⚠ / ✗ mark states judgement
# the trend -> kin-1 · never put the figure itself in a trigger colour unless
# the thing it measures is actually in alarm
