# Bars — compare a few values. A thin track with a gilded head, not a slab.

 rust    ───────────────●      62%
 python  ─────────●            38%
 shell   ───●                  12%
 lua     ─●                     6%

# dotted-rest variant (track stays visible):
 build   ────────────●┈┈┈┈┈┈   54%

# square variant, when you want a touch more body (still not solid):
 disk    ▰▰▰▰▰▰▰▱▱▱            71%

# paint: line/squares -> kin-1 (gilding); the head ● -> kin-0; rest -> ink-3
# label left and dim, value right and bone; longest bar sets the column
