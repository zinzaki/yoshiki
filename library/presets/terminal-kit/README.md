```
╭─ terminal-kit ───
│  a whole terminal in one move
╰╴
```

> a whole terminal in one move: emulator + prompt + glyphs, all
> night-beige, all from the library. A preset curates; it never redefines color.

What you get, together and coherent:
- **emulator** — kitty / foot / alacritty colors (pick one)
- **prompt** — the reference two-line `◆` starship capsule + its palette block
- **glyphs** — the flow/status rows the prompt and your tools lean on

Swap `theme: night-beige → beige-glass` in `preset.yml` for the light twin; everything
else stays identical. See `preset.yml` for exact paths and the apply steps.
